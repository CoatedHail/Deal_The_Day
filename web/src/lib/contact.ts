/**
 * Where messages from the site are addressed.
 *
 * These are deliberate placeholders on the example.com domain, which can never
 * receive mail — a wrong-but-plausible address would silently swallow feedback.
 * Replace them once the project's real addresses exist.
 */
export const FEEDBACK_EMAIL = "feedback@example.com";
